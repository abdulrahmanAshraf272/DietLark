import 'package:flutter/material.dart';

class NutrientSelected extends StatelessWidget {
  const NutrientSelected({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
